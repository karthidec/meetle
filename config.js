// Meetle — API Configuration
//
// This file is intentionally empty of any API key.
// Each user enters their own Google Maps API key in the app on first visit.
//
// If you are doing a PRIVATE internal deployment and want to pre-fill a key,
// you can add it here — but do NOT commit a real key to a public repo.

window.MEETLE_CONFIG = {
  MAPS_API_KEY: ''
};
